# WordPress Paths
WP_CONFIG_PATH="/var/www/example.com/wp-config.php"
WP_CONTENT_PATH="/var/www/example.com/htdocs/wp-content"
WP_TEMP_DIR="/var/tmp/wp-backup-work"

# MariaDB - Leave empty to auto-extract from wp-config.php
DB_HOST="localhost"
DB_NAME=""
DB_USER=""
DB_PASSWORD=""

# Mega Accounts (Up to 3 supported)
# Primary Account (Required)
MEGA_EMAIL_1="primary@example.com"
MEGA_PASSWORD_1="your-password-here"

# Secondary Account (Optional)
MEGA_EMAIL_2=""
MEGA_PASSWORD_2=""

# Tertiary Account (Optional)
MEGA_EMAIL_3=""
MEGA_PASSWORD_3=""

# Mega Storage Limit (GB) per account
# Default: 19.5 (allows 500MB buffer on free 20GB accounts)
MEGA_STORAGE_LIMIT_GB="19.5"

# SMTP Configuration (For email reports)
SMTP_SERVER="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="notifications@example.com"
SMTP_PASSWORD="app-specific-password"
SMTP_SENDER_EMAIL="backup-bot@example.com"

# Retention Settings
BACKUP_DIR="/opt/wordpress-backup/backups"
RETENTION_LOCAL_DAYS="2"
RETENTION_MEGA_DAYS="7"

# Database File (SQLite)
DB_FILE="backups.db"

# Cloudflare D1 Sync (Optional)
CLOUDFLARE_ACCOUNT_ID=""
CLOUDFLARE_API_TOKEN=""
CLOUDFLARE_D1_DATABASE_ID=""

# Deployment Settings (Used by deploy.sh)
REMOTE_HOST="your-server.com"
REMOTE_USER="ubuntu"
REMOTE_PORT="22"
REMOTE_DIR="/opt/wordpress-backup"
